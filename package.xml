<?xml version="1.0"?>
<?xml-model href="http://download.ros.org/schema/package_format3.xsd" schematypens="http://www.w3.org/2001/XMLSchema"?>
<package format="3">
  <name>rmw_robotops</name>
  <version>0.9.8</version>
  <description>
    ROS2 RMW implementation that wraps underlying RMW (atop various DDS implementations: FastDDS, CycloneDDS, etc.)
    to add distributed tracing capabilities with OpenTelemetry context propagation.
  </description>

  <maintainer email="kristophm@robotops.com">Kristoph Matthews</maintainer>
  <license>Apache-2.0</license>

  <buildtool_depend>ament_cmake</buildtool_depend>
  <buildtool_depend>ament_cmake_ros</buildtool_depend>

  <!-- Core RMW dependencies -->
  <depend>rcutils</depend>
  <depend>rmw</depend>
  <depend>rcpputils</depend>

  <!-- Message dependencies -->
  <depend version_gte="0.3.2">robotops_msgs</depend>
  <depend>rosidl_runtime_c</depend>
  <depend>rosidl_typesupport_introspection_c</depend>
  <depend>rosidl_typesupport_introspection_cpp</depend>

  <!-- Configuration dependencies -->
  <depend version_gte="0.4.15">robotops-config</depend>

  <!-- ros2_tracing for LTTng tracepoint emission -->
  <depend>tracetools</depend>

  <!-- FastDDS dependencies (can be swapped at runtime).
       No version floor: the underlying RMW is selected/loaded by library name
       at runtime (see src/rmw_init.cpp) and CMake imposes no FastDDS version,
       so these are plain deps. Pinning a floor here breaks Humble, which ships
       older FastDDS than Jazzy (fastrtps 2.6.x / fastcdr 1.0.x vs Jazzy's
       2.14.x / 2.x) — the old 2.10.0/2.2.0 floors were Jazzy-only (ROB-325). -->
  <depend>rmw_fastrtps_cpp</depend>
  <depend>rmw_fastrtps_shared_cpp</depend>
  <depend>fastrtps</depend>
  <depend>fastcdr</depend>

  <!-- YAML parsing for configuration -->
  <depend>yaml-cpp</depend>

  <!-- xxHash for content hashing (system dependency) -->

  <!-- Testing dependencies -->
  <test_depend>ament_lint_auto</test_depend>
  <test_depend>ament_lint_common</test_depend>
  <test_depend>ament_cmake_gtest</test_depend>
  <test_depend>rmw_implementation_cmake</test_depend>
  <test_depend>std_msgs</test_depend>

  <export>
    <build_type>ament_cmake</build_type>
    <rmw_typesupport>cpp</rmw_typesupport>
  </export>
</package>
